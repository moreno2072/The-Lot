import { NextRequest, NextResponse } from 'next/server'; import Stripe from 'stripe'; import { prisma } from '@/lib/prisma'; import { getSession } from '@/lib/auth'; const stripe = process.env.STRIPE_SECRET_KEY ? new Stripe(process.env.STRIPE_SECRET_KEY) : null; export async function GET(req: NextRequest) { const session = await getSession(); const origin = req.nextUrl.origin; if (!session || !stripe) { return NextResponse.redirect(`${origin}/dashboard`); } const store = await prisma.store.findUnique({ where: { sellerId: session.userId } }); if (store?.stripeAccountId) { const account = await stripe.accounts.retrieve(store.stripeAccountId); await prisma.store.update({ where: { id: store.id }, data: { stripeOnboarded: Boolean(account.payouts_enabled) }, }); } return NextResponse.redirect(`${origin}/dashboard`); }
