'use client'; import { useState } from 'react'; export default function ConnectPayoutsButton({ onboarded }: { onboarded: boolean }) { const [loading, setLoading] = useState(false); async function handleClick() { setLoading(true); const res = await fetch('/api/stripe/connect', { method: 'POST' }); const data = await res.json(); setLoading(false); if (data.url) { window.location.href = data.url; } else { alert(data.error || 'Could not start payout setup.'); } } if (onboarded) { return ( <span className="inline-flex items-center gap-1.5 rounded border border-hairline/20 px-3 py-1.5 font-mono text-xs uppercase tracking-widest text-ink/60"> <span className="h-1.5 w-1.5 rounded-full bg-green-600" /> Payouts connected </span> ); } return ( <button onClick={handleClick} disabled={loading} className="rounded bg-gavel px-3 py-1.5 font-mono text-xs uppercase tracking-widest text-ink hover:opacity-90 transition-opacity disabled:opacity-50" > {loading ? 'Redirecting…' : 'Connect payouts'} </button> ); }
